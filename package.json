{
  "name": "vexa",
  "private": true,
  "version": "0.1.0",
  "description": "AI job application copilot — draft + one-tap, never auto-submit bots",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "pnpm --filter @vexa/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "e2e": "node scripts/e2e-smoke.mjs",
    "e2e:pipeline": "node scripts/e2e-pipeline.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
